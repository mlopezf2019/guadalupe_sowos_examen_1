1. 
isbn = input("Escribe el ISBN: ")
isbn = isbn.replace("-", "")
total = 0
for i in range(10):
  digit = "10" if isbn[i] == "10" else isbn[i]
  total+= int(digit) * (10 - i)
print ("ISBN Valid: ", (total % 11) == 0)

2.
def factorial(number):
  res = 1
  for i in range(1, number + 1): 
    res = res * i 
  return res
  
 Complejidad (N/A)
 
 3.
